{
  "buildCommand": "cd ../../.. && pnpm install --recursive --no-frozen-lockfile && cd frontend/apps/tenant && pnpm run build",
  "outputDirectory": "dist",
  "devCommand": "pnpm dev",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
